#ifndef ${guard=HEADER_GUARD}
#define ${guard}

namespace ${namespace=NAMESPACE} {

template <typename T>
class ${class_name=NAME} {
public:
    ${class_name}();
    ~${class_name}();

private:

};

} // namespace ${namespace}

#endif // ${guard}
