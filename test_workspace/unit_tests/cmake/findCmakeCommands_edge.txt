set(LIB "add_library (lib ${SOURCES})")

add_library(lib STATIC
    base/Allocator.cpp
    base/BitOperations.cpp
)

# add_executable(${CMAKE_PROJECT_NAME} main.cpp)

target_link_libraries(${CMAKE_PROJECT_NAME} PRIVATE
    lib
)
